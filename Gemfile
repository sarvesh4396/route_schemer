# frozen_string_literal: true

source "https://rubygems.org"

# Specify your gem's dependencies in route_schemer.gemspec
gemspec

# CI pins a Rails version here; local dev resolves normally.
gem "rails", ENV["RAILS_VERSION"] if ENV["RAILS_VERSION"]
